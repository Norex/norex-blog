<div id="tags-block" class="block categories">
  <h2>Tags <a href="#">all</a></h2>
  <div class="divider dark">&nbsp;</div>
  <ul id="category-list">
    <% Tumble.tag_counts.each do |tag| %>
      <li><%= tag_link_to(tag.name, tag.count) %></li>
    <% end %>
  </ul>
</div>